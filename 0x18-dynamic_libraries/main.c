#include "main.h"
#include <stdio.h>

/**
 * main -this is to  check the code
 *
 * Return: Always return  EXIT_SUCCESS.
 */
int main(void)
{
    printf("%d\n", _strlen("My Dyn Lib"));
    return (EXIT_SUCCESS);
}
